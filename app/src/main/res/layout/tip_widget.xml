<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tip_widget_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/stars_background"
    android:orientation="horizontal"
    android:padding="@dimen/widget_margin">

    <ImageView
        android:layout_width="@dimen/widget_iv_dimen"
        android:layout_height="@dimen/widget_iv_dimen"
        android:background="@color/pink200"
        android:padding="@dimen/widget_iv_padding"
        android:contentDescription="@string/description_app_logo"
        android:src="@drawable/ic_soap" />

    <TextView
        android:id="@+id/widget_title"
        style="@style/TitleStyle"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/widget_iv_dimen"
        android:duplicateParentState="true"
        android:ellipsize="marquee"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:gravity="center"
        android:marqueeRepeatLimit="marquee_forever"
        android:padding="@dimen/widget_desc_padding"
        android:scrollHorizontally="true"
        android:singleLine="true"
        android:text="@string/widget_title"
        android:textColor="@color/bluegray700" />

    <requestFocus
        android:duplicateParentState="true"
        android:focusable="true"
        android:focusableInTouchMode="true" />
</LinearLayout>